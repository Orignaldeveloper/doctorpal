spring.application.name=doctorpal
# MongoDB
#spring.data.mongodb.uri=${MONGO_URI:mongodb://localhost:27017/doctorpal}
#spring.data.mongodb.uri=mongodb+srv://doctorpal_dev:Originals777@transport-cluster.aqzl8dv.mongodb.net/doctorpal?appName=transport-cluster
spring.data.mongodb.database=${MONGO_DB:doctorpal}

# MongoDB
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://doctorpal_dev:Originals777@transport-cluster.aqzl8dv.mongodb.net/doctorpal?appName=transport-cluster}
spring.data.mongodb.database=${MONGO_DB:doctorpal}

# JWT
app.jwt.secret=${JWT_SECRET:DoctorPalSuperSecretKeyForJWTTokenGeneration2024!}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# Server
server.port=${PORT:8080}

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Super Admin credentials (set these in Render environment variables)
app.admin.email=${ADMIN_EMAIL:satyamsuper77@gmail.com}
app.admin.password=${ADMIN_PASSWORD:Docpal!777}
app.admin.name=${ADMIN_NAME:Satyam Super Admin}

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Logging
logging.level.com.doctorpal=INFO
logging.level.org.springframework.security=WARN